<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userForm">

    <select id="userList" resultType="com.fastcampus.gearshift.dto.NUserFormDto">
        SELECT
            user_id,
            user_name,
            user_account,
            user_phone_number,
            user_email,
            created_at
        FROM user
        ORDER BY user_id
    </select>

    <select id="getUserFormById" resultType="com.fastcampus.gearshift.dto.NUserFormDto">
        SELECT
            user_id,
            user_name,
            user_account,
            user_phone_number,
            user_email,
            created_at
        FROM user
        WHERE user_id = #{userId}
    </select>

    <update id="updateUserForm" parameterType="com.fastcampus.gearshift.dto.NUserFormDto">
        UPDATE user
        SET
            user_name = #{userName},
            user_password = #{userPassword},
            user_email = #{userEmail},
            user_phone_number = #{userPhoneNumber}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser" parameterType="String">
        UPDATE user
        SET is_deleted = 1
        WHERE user_id = #{userId}
    </delete>

</mapper>
